# CalvisionTB

Simple analysis of RECO files from CALVISION TestBeam at FNAL in April 2023

One need to change the path to data files in ```calvisinTB.cc```

Then compile and run, specify run range
```
make
./calvisionTB 75145 75167
```

The output root file with histograms is ```output.root```
